meta {
  name: Capture Authorization
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/v1/cards/capture
}

body:json {
  {
    "auth_id": "{{authId}}"
  }
}

assert {
  res.status: eq 201
  res.body.status: eq completed
}
