FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY lynx_simulator.py .

# All tunables come from env (see lynx_simulator.py docstring).
ENTRYPOINT ["python", "lynx_simulator.py"]
